[build]
  command = "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @oplata/frontend run build"
  publish = "apps/frontend/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
